<nav class="top-nav navbar navbar-default navbar-fixed-top">
  <div class="row">

    <div class="col-md-9 navbar-header">
      <p>
        <a class="home-link" href="<%=root_path%>">DENVER: RE<span class="non-bold">IMAGINE</span></a>
      </p>
    </div>

    <div class="col-md-3">
      <ul class="top-nav-list">
        <% if current_user %>
          <li>Signed in as <strong><%= current_user.name %></strong></li>
          <li><%= link_to "Sign out", signout_path, id: "sign_out" %></li>
        <% else %>
          <li><%= link_to "Sign in with Google", "/auth/google_oauth2", id: "sign_in" %></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>